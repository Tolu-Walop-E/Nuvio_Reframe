package com.nuvio.tv.ui.screens.home.netflix

internal object NetflixHomeFeature {
    const val ENABLED = true

    /** Netflix UI always autoplays trailers when the build allows in-app trailers. */
    const val FORCE_TRAILER_AUTOPLAY = true
}
